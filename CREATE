CREATE TABLE COLLEGE_STUDENT (
    ROLL_NO NUMBER CONSTRAINT pk_roll PRIMARY KEY,
    FULL_NAME VARCHAR2(60) NOT NULL,
    DEPT VARCHAR2(25),
    COURSE_DURATION NUMBER(2),
    HOME_ADDRESS VARCHAR2(100),
    CONTACT_NO VARCHAR2(15) UNIQUE
);
SELECT * FROM COLLEGE_STUDENT;

